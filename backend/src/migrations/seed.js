/**
 * NOTA: Este archivo se mantiene por compatibilidad con rutas existentes.
 * El script de seed canónico está en: database/scripts/seed.js
 *
 * Ejecución recomendada:
 *   node database/scripts/seed.js   (desde la raíz del proyecto)
 *
 * O desde el directorio backend:
 *   node src/migrations/seed.js
 */

// Redirige al script canónico
require('../../../../database/scripts/seed.js');
